# array_keys(配列)
配列のkeyのみを表示する

# array_values(配列)
配列のvalueのみを表示する

# array_key_exists(探したいkey, 配列)
探したいkeyが配列にあればtrueを返す

# in_array(探したいvalue, 配列)
探したいvalueが配列にあればtrueを返す

# array_search(value, 配列)
valueを検索して対応するkeyを返す
