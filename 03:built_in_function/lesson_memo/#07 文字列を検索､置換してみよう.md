# preg_match(検索したいパターン, 検索したい文字列, 検索した結果を格納するための変数)
文字列から検索できる

# patternに正規表現を入れる

# print_r
指定した変数に関する情報を解りやすく出力する

# preg_match_all();
一致する文字列を全て格納できる

# preg_replace(パターン, 置換後も文字列, 検索したい文字列);
パターンに一致した文字列を指定の文字列に置換する
